{
  "scripts": {
    "start": "time node ./packages/exchanges-miner/dist/index",
    "bootstrap": "lerna bootstrap",
    "build": "lerna bootstrap && lerna run --ignore exchanges-miner tsc && lerna run build"
  },
  "devDependencies": {
    "lerna": "^2.2.0",
    "typescript": "^2.5.3"
  }
}
