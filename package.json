{
  "scripts": {
    "start": "time node ./packages/exchanges-miner/dist/index",
    "build": "lerna bootstrap && lerna run build"
  },
  "devDependencies": {
    "lerna": "^2.2.0",
    "typescript": "^2.5.3"
  }
}
